two different way to define Array.

const cars = ['Saab', 'Volvo', 'BMW', 3453, null, () => {}];

const cars = new Array('Saab', 'Volvo', 'BMW', 3453, null, () => {});

-----------sort start------------
note : Arrays use numbers to access its "elements". In this example, person[0] returns John:
note : Objects use names to access its "members". In this example, person.firstName returns John:
if you want to know more about sort method.. then you have to understand the insertion sort algo.
-----------sort end------------

sort method and length method

insert value at last index
